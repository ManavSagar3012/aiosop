# MCP tooling-reality qualification suite.
#
# These tests assert that each "REAL" MCP server actually executes real tools.
# They are the CI gate that fails if a server silently reverts to a stub/mock.
